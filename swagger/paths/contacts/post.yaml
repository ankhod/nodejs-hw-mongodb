post:
  tags:
    - Contacts
  summary: Create a new contact
  operationId: createContact
  description: Create a new contact with the provided data.
  security:
    - bearerAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            name:
              type: string
              example: 'Jane Doe'
            email:
              type: string
              example: 'jane@example.com'
  responses:
    '201':
      description: Contact created successfully
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                example: 201
              message:
                type: string
                example: 'Contact created successfully'
              data:
                type: object
                properties:
                  _id:
                    type: string
                    example: '683b67a18592f67fa7b753e4'
                  name:
                    type: string
                    example: 'Jane Doe'
                  email:
                    type: string
                    example: 'jane@example.com'
    '400':
      description: Invalid input data
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                example: 400
              message:
                type: string
                example: 'Invalid input data'
